DA GAMEBOARD
<% racers.forEach(function(racer) { %>
	<div class="racer" data-id="<%= racer.user_id %>">
		<span class="racer_name"><%= racer.escape("user_id") %></span>
		<span clas="racer_car"><%= racer.progress %></span>
	</div>
<% }) %>